{
    # Global options
    email your-email@example.com
}

# Replace 'example.com' with your actual domain name
# If you don't have a domain yet, use 'localhost' for local HTTPS testing (Caddy will generate a local CA cert)
:443 {
    reverse_proxy web:80
}
